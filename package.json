{
  "name": "convert-weblate-csv",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "build": "esbuild src/main.ts --bundle --platform=node --target=node24 --outfile=dist/index.js"
  },
  "dependencies": {
    "csv-parser": "^3.2.0",
    "csvwriter": "^1.2.0",
    "strip-bom-stream": "^5.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "esbuild": "^0.28.0",
    "typescript": "^6.0.0"
  },
  "engines": {
    "node": ">=24.15"
  }
}
